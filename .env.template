# Poe API Key
# Get your API key from: https://poe.com/api_key
POE_API_KEY=your_poe_api_key_here

# LiteLLM Master Key (for securing the proxy)
# Must start with 'sk-' prefix
# Generate with: openssl rand -hex 32
LITELLM_MASTER_KEY=sk-your_secure_master_key_here

# Port to expose the proxy on (default: 4000)
LITELLM_PORT=4000

# Tailscale Auth Key (REQUIRED)
# Get from: https://login.tailscale.com/admin/settings/keys
# Create a reusable key with tag "tag:container"
TAILSCALE_AUTHKEY=tskey-auth-xxxxx
